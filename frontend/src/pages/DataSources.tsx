import React from 'react';
import { Typography } from '@mui/material';

const DataSources: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">DataSources Page</Typography>
      <Typography>Content for DataSources will be implemented here.</Typography>
    </div>
  );
};

export default DataSources;
