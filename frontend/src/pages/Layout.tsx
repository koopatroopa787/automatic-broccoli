import React from 'react';
import { Typography } from '@mui/material';

const Layout: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Layout Page</Typography>
      <Typography>Content for Layout will be implemented here.</Typography>
    </div>
  );
};

export default Layout;
