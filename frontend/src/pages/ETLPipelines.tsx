import React from 'react';
import { Typography } from '@mui/material';

const ETLPipelines: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">ETLPipelines Page</Typography>
      <Typography>Content for ETLPipelines will be implemented here.</Typography>
    </div>
  );
};

export default ETLPipelines;
