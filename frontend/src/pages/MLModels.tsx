import React from 'react';
import { Typography } from '@mui/material';

const MLModels: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">MLModels Page</Typography>
      <Typography>Content for MLModels will be implemented here.</Typography>
    </div>
  );
};

export default MLModels;
